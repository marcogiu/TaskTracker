import { Flex } from '@chakra-ui/react';

export const Calendar = (): JSX.Element => {
  return <Flex w='95%' m='0 auto' flexDir='column' p={5} borderWidth={2} borderColor='button' borderRadius='md' bgColor='cardBackground'></Flex>;
};
