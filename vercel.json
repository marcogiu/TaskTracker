{
  "version": 2,
  "projects": [
    {
      "name": "client",
      "root": "client"
    },
    {
      "name": "server",
      "root": "server"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server/$1" },
    { "src": "/(.*)", "dest": "/client/$1" }
  ]
}
